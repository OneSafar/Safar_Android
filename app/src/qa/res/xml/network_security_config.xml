<?xml version="1.0" encoding="utf-8"?>
<!--
  QA-flavor-only override of app/src/main/res/xml/network_security_config.xml.
  Adds cleartext HTTP for local-dev/LAN backends (SAFAR_QA_BASE_URL in
  local.properties) so the QA build can talk to `npm run dev` without
  deploying. Prod is untouched — it keeps main's cleartextTrafficPermitted="false".
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">safar.parmarssc.in</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Local-dev backend: emulator loopback alias + common private LAN ranges. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.200.50.18</domain>
    </domain-config>
</network-security-config>
