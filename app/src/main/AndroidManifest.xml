<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Allow PackageManager to resolve launchable apps for the app picker. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".SafarApplication"
        android:allowBackup="${allowBackup}"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:icon="@mipmap/ic_safar_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_safar_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Safar"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true"
            android:screenOrientation="portrait"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="safar" />
            </intent-filter>
        </activity>

        <!-- Dedicated full-screen video player Activity.
             Plain Android Activity (no Compose) so the WebView's SurfaceView
             can composite video frames correctly without any Compose layering conflicts. -->
        <activity
            android:name=".feature.live.presentation.VideoPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.Safar"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden" />



        <service
            android:name=".notifications.SafarFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM default icon: ensures the monochrome sparkle is used on ALL phones,
             even when FCM auto-displays the notification (background/killed state).
             Without this, OEMs fall back to the launcher icon → pitch-black square. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_safar_notification_sparkle" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/safar_notification_color" />

        <service
            android:name=".ui.ekagra.TimerService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <!-- Supporting locale sync and persist for pre-Android 13 devices using AppCompat -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="androidx.appcompat.app.AppLocalesMetadataHolderService" />
            </intent-filter>
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <service
            android:name=".ui.ekagra.focusshield.FocusShieldAccessibilityService"
            android:exported="true"
            android:label="@string/focus_shield_accessibility_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/focus_shield_accessibility_service" />
        </service>

        <service
            android:name=".ui.ekagra.focusshield.FocusShieldNotificationListenerService"
            android:exported="true"
            android:label="@string/focus_shield_notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity
            android:name=".ui.ekagra.focusshield.BlockedAppActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Safar" />

        <!-- Reschedule notification workers after device boot or app update.
             Cat-7 P1 fix: ensures study reminders, planner alerts, and morning nudges
             survive device reboots and app updates without waiting for cold launch. -->
        <receiver
            android:name=".notifications.NotificationRescheduleReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
